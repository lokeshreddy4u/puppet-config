/var/log/puppet/*log {
  missingok
  notifempty
  create 0644 puppet puppet
  sharedscripts
  postrotate
    [ -e /etc/init.d/pe-puppet ] && /etc/init.d/pe-puppet reload > /dev/null 2>&1 || true
  endscript
}

